import React from 'react'
import ReactDOM from 'react-dom/client'

function App() {
  return <h1 style={{textAlign: 'center', marginTop: '100px'}}>ATK Capital Mockup ✅</h1>
}

ReactDOM.createRoot(document.getElementById('root')!).render(<App />)